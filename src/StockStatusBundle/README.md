# StockStatus bundle

This minimal bundle provides a simple `StockService` and an API endpoint at `/api/stock-status`.

It is intentionally minimal — replace `StockService` with a real provider for production use.
